# kb_writer

Purpose: Write validated cards into Obsidian KB.

Rules:
- Never overwrite the whole card unless explicitly requested.
- Append or replace a delimited Grok API expansion section.
- Rebuild index after apply.
- Run validate after apply.
- Keep source URLs and collected_at metadata.
