# Query Bank

## Meta Seeds

```text
("bug bounty" OR bugbounty) (writeup OR "case study" OR "root cause") since:2024-01-01 -is:retweet lang:en
("bug bounty" ("new technique" OR trick OR methodology OR workflow)) since:2024-01-01 -is:retweet lang:en
```

## Topic Queries

```text
(recon OR reconnaissance OR "asset discovery" OR "attack surface") "bug bounty" since:2024-01-01 -is:retweet lang:en
(IDOR OR BOLA OR "access control") ("bug bounty" OR hackerone OR bugcrowd OR intigriti) since:2024-01-01 -is:retweet lang:en
(OAuth OR SSO OR SAML OR JWT OR "magic link") (bypass OR vulnerability OR "bug bounty") since:2024-01-01 -is:retweet lang:en
(GraphQL OR introspection OR batching OR "API schema") (authorization OR IDOR OR "bug bounty") since:2024-01-01 -is:retweet lang:en
("multi-tenant" OR tenant OR workspace OR org_id OR organization) (IDOR OR authorization OR "bug bounty") since:2024-01-01 -is:retweet lang:en
(billing OR payment OR subscription OR invoice OR coupon OR refund OR webhook) "bug bounty" since:2024-01-01 -is:retweet lang:en
(postMessage OR DOMPurify OR CSPT OR "client-side" OR prototype) "bug bounty" since:2024-01-01 -is:retweet lang:en
("web cache" OR "cache poisoning" OR "cache deception" OR "request smuggling" OR HTTP2 OR HTTP3) "bug bounty" since:2024-01-01 -is:retweet lang:en
("GitHub Actions" OR CI/CD OR "build artifact" OR S3 OR GCS OR Azure Blob OR cloud) "bug bounty" since:2024-01-01 -is:retweet lang:en
("prompt injection" OR LLM OR "tool calling" OR "AI security") "bug bounty" since:2024-01-01 -is:retweet lang:en
("old bug" OR evergreen OR "still works" OR "classic bug") "bug bounty" since:2024-01-01 -is:retweet lang:en
("bug bounty" (trick OR tip OR "TIL" OR "weird" OR bypass)) since:2024-01-01 -is:retweet lang:en
```
